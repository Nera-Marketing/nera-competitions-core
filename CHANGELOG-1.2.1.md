# Nera Competitions Standard 1.2.1

## Purchase card & quantity

- **Slider quantity layout — tap to edit:** tap the ticket count on slider layout to type a quantity directly (Enter to confirm, Escape to cancel, blur commits).
- **QuantitySelector block:** `edit_quantity` i18n string; clickable display trigger and hidden number input in `template.twig`.
- **`quantity-control.js`:** `enterEdit`, `commitEdit`, and `exitEdit` handlers wired to display trigger and edit input.

## Admin

- Minor update to `assets/js/admin-page-components-search.js`.
